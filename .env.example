# Postgres connection string (local docker-compose default below; use your Neon URL in production)
DATABASE_URL="postgresql://docsy:docsy@localhost:5433/docsy"

# Secret used to sign session cookies. Generate one with: openssl rand -hex 32
SESSION_SECRET="dev-only-secret-change-in-production"
